# ?????
server.port=8080

# Spring ????
spring.application.name=PawHub

# ?????
spring.datasource.url=jdbc:mysql://localhost:3306/pawhub?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=Root@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MyBatis ??
mybatis.configuration.map-underscore-to-camel-case=true
mybatis.mapper-locations=classpath:mappers/*xml
mybatis.type-aliases-package=com.jtyu.backend.mybatis.entity

# ??????
file.base-path=D:/IDEAproject/backend

# DeepSeek API ??
deepseek.api.key=${DEEPSEEK_API_KEY}
deepseek.api.url=https://api.deepseek.com/v1
deepseek.model=deepseek-chat

# JWT Secret?Base64 ??????
jwt.secret=${JWT_SECRET}

# CORS ??
cors.allowed.origins=${CORS_ALLOWED_ORIGINS}
cors.allowed.methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
cors.max.age=3600
